/** @type {import("next").NextConfig} */
const config = {
  // webpack: (config) => {
  //   config.externals = [...config.externals, "bcrypt"];
  //   return config;
  // },
};

export default config;
